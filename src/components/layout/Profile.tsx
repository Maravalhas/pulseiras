import { Container } from "react-bootstrap";

const Profile = () => {
  return (
    <div className="profile">
      <Container>Bem vindo, Marco</Container>
    </div>
  );
};

export default Profile;
